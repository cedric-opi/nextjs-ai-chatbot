# Allows pnpm to hoist all packages to root node_modules
# This is "shameful" because it breaks pnpm's isolation, but fixes these issues
shamefully-hoist=true
# Specifically hoist Radix UI packages
public-hoist-pattern[]=*@radix-ui/*
# Automatically install peer dependencies
auto-install-peers=true
# Don't fail on peer dependency conflicts
strict-peer-dependencies=false
# Use hoisted node linker (similar to npm/yarn)
node-linker=hoisted